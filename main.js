// main.js

// Uso de los componentes simulados
const headerComponent = new Header('Encabezado','Mi Composicion de Componentes');
const listComponent = new CustomList(['Lista','Elemento 1', 'Elemento 2', 'Elemento 3']);
const formComponent = new CustomForm([{ type: 'text', label: 'Nombre' }, { type: 'email', label: 'Correo' }]);
